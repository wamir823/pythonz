name: Update Currency Data

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'  # Runs every 30 minutes

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run Data Fetcher
        run: |
          # دانلود و نصب Telethon
          wget https://github.com/LonamiWebs/Telethon/archive/refs/heads/v1.zip
          rm -rf Telethon*
          unzip v1.zip
          rm -rf v1.zip
          cd Telethon*
          cp -rf * ../
          cd ..
          rm -rf Telethon*
          rm -rf api.tl*
          rm -rf telethon_generator/data/api.tl*
          
          # دانلود schema جدید
          wget https://raw.githubusercontent.com/telegramdesktop/tdesktop/dev/Telegram/SourceFiles/mtproto/scheme/api.tl
          mv api.tl telethon_generator/data/api.tl
          
          # استخراج نسخه و ساخت فایل tdesktop.py
          VERSION=$(curl -s https://raw.githubusercontent.com/telegramdesktop/tdesktop/dev/Telegram/SourceFiles/core/version.h | grep -oP 'AppVersionStr = "\K[^"]+' | head -1)
          echo "نسخه تشخیص داده شده: $VERSION"
          
          # ایجاد دایرکتوری و فایل
          mkdir -p telethon/events
          echo "def tdesktop():" > telethon/events/tdesktop.py
          echo "    return \"$VERSION\"" >> telethon/events/tdesktop.py
          echo "tdesktop()" >> telethon/events/tdesktop.py
          # نمایش محتوای فایل ساخته شده
          cat telethon/events/tdesktop.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --quiet && git diff --staged --quiet || (git commit -m "🚀 Updated - $(TZ='Asia/Tehran' date '+%Y-%m-%d %H:%M:%S')" && git push)
